class ApiConfig {
  // If using an Android Emulator, use 10.0.2.2 to reach the Windows localhost.
  // If using a physical device, use your PC's Wi-Fi IP address (e.g., 192.168.x.x).
  static const String baseUrl = 'http://172.18.4.27:8000/api';
  static const double geofenceRadius = 10.0;
}